[package]
name = "x264-sys"
version = "0.1.0"
edition = "2018"
build = "build.rs"

[build-dependencies]
# We're very permissive here with bindgen due to https://github.com/rust-lang/cargo/issues/5237
bindgen = "0.*"
cc = "1.0"
pkg-config = "0.3.25"
