[package]
name = "av-foundation"
version = "0.1.0"
edition = "2018"
build = "build.rs"

[dependencies]
foundation = { path = "../foundation" }
core-foundation = { path = "../core-foundation" }
core-media = { path = "../core-media" }

[build-dependencies]
cc = "1.0"
